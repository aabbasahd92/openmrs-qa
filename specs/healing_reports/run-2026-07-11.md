# Test Failure Classification Report

- **Generated:** 2026-07-14T00:10:23.311300+00:00
- **Model:** claude-sonnet-4-6
- **Total failures classified:** 25

> ⚠️ This report contains SUGGESTED fixes only. No test code has been modified.
> A human must review each suggestion before applying it.

## Summary

| Category | Count |
|---|---|
| ENVIRONMENT_ISSUE | 25 |
| LOCATOR_DRIFT | 0 |
| REAL_DEFECT | 0 |
| TIMING_ISSUE | 0 |
| UNKNOWN | 0 |

## 🌐 ENVIRONMENT_ISSUE — target environment problem, not a code defect

### `tests/test_api_patients.py::test_api_auth_valid`
**Rationale:** Every API request returns HTTP 403, indicating the target environment is blocking all requests (e.g., firewall rule, IP allowlist, bot-challenge, or the service is down/misconfigured) rather than a test or application logic defect.

**Suggested fix:** Verify the target API base URL is reachable from the CI/test runner, check firewall/IP allowlist rules, confirm credentials and authentication headers are correctly configured in the test environment, and ensure the OpenMRS instance is running and accessible.

### `tests/test_api_patients.py::test_api_auth_invalid`
**Rationale:** Returns HTTP 403 even for the invalid-auth test case, which should return 401; this uniform 403 across all tests points to the environment blocking requests wholesale rather than the app responding to auth logic.

**Suggested fix:** Same as test_api_auth_valid: verify the environment is reachable and not behind a firewall or bot-challenge that intercepts all requests before they reach OpenMRS.

### `tests/test_api_patients.py::test_api_search_patient_returns_results`
**Rationale:** HTTP 403 returned for a search endpoint, consistent with all other API tests; the entire API surface is inaccessible from the test runner.

**Suggested fix:** Verify network access to the OpenMRS REST API endpoint from the test runner; check proxy, VPN, or IP allowlist configuration.

### `tests/test_api_patients.py::test_api_search_patient_response_shape`
**Rationale:** HTTP 403 returned, consistent with a blanket environment-level block rather than an application or test logic issue.

**Suggested fix:** Verify network access to the OpenMRS REST API endpoint from the test runner; check proxy, VPN, or IP allowlist configuration.

### `tests/test_api_patients.py::test_api_get_patient_by_uuid`
**Rationale:** The search step returns an empty or non-JSON body (JSONDecodeError on empty string), which is consistent with the environment returning a 403/HTML error page instead of JSON, causing the upstream search call to fail before the UUID lookup even runs.

**Suggested fix:** Resolve the environment-level 403 blocking all API calls (same root cause as sibling tests); once the environment is accessible, this test should receive valid JSON responses.

### `tests/test_api_patients.py::test_api_search_no_results`
**Rationale:** HTTP 403 returned, consistent with a blanket environment-level block affecting all API tests in this run.

**Suggested fix:** Verify network access to the OpenMRS REST API endpoint from the test runner; check proxy, VPN, or IP allowlist configuration.

### `tests/test_login.py::test_valid_login[chromium]`
**Rationale:** The locator '#username' times out after 20 seconds, which combined with the API tests all returning 403 strongly indicates the OpenMRS O3 web application is unreachable or returning a non-login page (e.g., a bot-challenge or error page) rather than the login form.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running; add a page.goto() response status check before waiting for the username locator to surface environment failures early.

### `tests/test_login.py::test_full_login_with_location[chromium]`
**Rationale:** Identical timeout on '#username' as all other UI tests; the environment is not serving the login page.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_login.py::test_invalid_login[chromium]`
**Rationale:** Identical timeout on '#username' as all other UI tests; the environment is not serving the login page.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_login.py::test_empty_username[chromium]`
**Rationale:** Identical timeout on '#username' as all other UI tests; the environment is not serving the login page.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_chart.py::test_patient_chart_loads[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_chart.py::test_patient_chart_shows_vitals[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_chart.py::test_patient_chart_shows_conditions[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_chart.py::test_patient_chart_shows_medications[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_chart.py::test_patient_chart_url_structure[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_registration.py::test_register_new_patient[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_registration.py::test_register_female_patient[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_registration.py::test_registration_page_loads[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_registration_ddt.py::test_register_patient_from_json[chromium-Alice]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_search.py::test_search_icon_visible[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_search.py::test_search_overlay_opens[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_search.py::test_search_returns_results[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_search.py::test_search_no_results[chromium]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_registration_ddt.py::test_register_patient_from_json[chromium-Bob]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

### `tests/test_patient_registration_ddt.py::test_register_patient_from_json[chromium-Fatima]`
**Rationale:** Setup fixture fails with the same '#username' timeout seen across all UI tests, indicating the target environment is not serving the application.

**Suggested fix:** Confirm the base URL is correct and the OpenMRS O3 instance is running and serving the login page at the configured URL.

## Human Review Decision

- [ ] Reviewed each suggested fix above
- [ ] Applied fixes manually where appropriate (no auto-apply)
- [ ] Confirmed REAL_DEFECT items are tracked as actual bugs, not test issues
- Reviewer: ______________________
- Date: ______________________
